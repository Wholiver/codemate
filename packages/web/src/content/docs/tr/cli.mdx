---
title: CLI
description: codemate CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

codemate CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
codemate
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, codemate ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
codemate run "Explain how closures work in JavaScript"
```

---

### tui

codemate terminal kullanıcı arayüzünü başlatır.

```bash
codemate [project]
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                    |
| ------------ | ---- | --------------------------------------------------------------------------- |
| `--continue` | `-c` | Son oturuma devam et                                                        |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                 |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| `--prompt`   |      | Kullanılacak prompt                                                         |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                 |
| `--agent`    |      | Kullanılacak agent                                                          |
| `--port`     |      | Dinlenecek port                                                             |
| `--hostname` |      | Dinlenecek host adı                                                         |

---

## Komutlar

codemate CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

codemate için agent'ları yönetin.

```bash
codemate agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir codemate arka uç sunucusuna bir terminal ekleyin.

```bash
codemate attach [url]
```

Bu, TUI öğesinin uzak bir codemate arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
codemate web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
codemate attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak      | Kısa | Tanım                                |
| ----------- | ---- | ------------------------------------ |
| `--dir`     |      | TUI'yi başlatmak için çalışma dizini |
| `--session` | `-s` | Devam edecek oturum açma bilgileri   |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
codemate agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
codemate agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
codemate auth [command]
```

---

#### login

codemate, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `codemate auth login` kullanabilirsiniz. Bu `~/.local/share/codemate/auth.json`'de saklanıyor.

```bash
codemate auth login
```

codemate başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
codemate auth list
```

Veya kısa versiyonu.

```bash
codemate auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
codemate auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
codemate github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
codemate github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
codemate github run
```

##### Bayraklar

| Bayrak    | Açıklama                                    |
| --------- | ------------------------------------------- |
| `--event` | Aracıyı çalıştırmak için GitHub sahte olayı |
| `--token` | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
codemate mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
codemate mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
codemate mcp list
```

Veya kısa versiyonunu kullanın.

```bash
codemate mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
codemate mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
codemate mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
codemate mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
codemate mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
codemate mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
codemate models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
codemate models anthropic
```

#### Bayraklar

| Bayrak      | Tanım                                                                       |
| ----------- | --------------------------------------------------------------------------- |
| `--refresh` | Modeller.dev'den model önbelleğini yenileyin                                |
| `--verbose` | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları codemate'da görmek istediğinizde kullanışlıdır.

```bash
codemate models --refresh
```

---

### run

Doğrudan bir istem ileterek codemate'u etkileşimli olmayan modda çalıştırın.

```bash
codemate run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "codemate run"
codemate run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `codemate serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
codemate serve

# In another terminal, run commands that attach to it
codemate run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                          |
| ------------ | ---- | --------------------------------------------------------------------------------- |
| `--command`  |      | Çalıştırılacak komut, args için mesajı kullanın                                   |
| `--continue` | `-c` | Son oturuma devam et                                                              |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                       |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)       |
| `--share`    |      | Oturumu paylaşın                                                                  |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                       |
| `--agent`    |      | Kullanılacak temsilci                                                             |
| `--file`     | `-f` | Mesaja eklenecek dosya(lar)                                                       |
| `--format`   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                |
| `--title`    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)          |
| `--attach`   |      | Çalışan bir codemate sunucusuna ekleyin (ör. http://localhost:4096)               |
| `--port`     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır) |

---

### serve

API erişimi için headless bir codemate sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
codemate serve
```

Bu, TUI arayüzü olmadan codemate işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `CODEMATE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `codemate` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

codemate oturumlarını yönetin.

```bash
codemate session [command]
```

---

#### list

Tüm codemate oturumlarını listeleyin.

```bash
codemate session list
```

##### Bayraklar

| Bayrak        | Kısa | Tanım                                  |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | En son N oturumla sınırla              |
| `--format`    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

codemate oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
codemate stats
```

#### Bayraklar

| Bayrak      | Açıklama                                                                                                    |
| ----------- | ----------------------------------------------------------------------------------------------------------- |
| `--days`    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| `--tools`   | Gösterilecek araç sayısı (tümü)                                                                             |
| `--models`  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| `--project` | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
codemate export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya codemate paylaşımından URL oturum verilerini içe aktarın.

```bash
codemate import <file>
```

Yerel bir dosyadan veya codemate paylaşımından (URL) içe aktarabilirsiniz.

```bash
codemate import session.json
codemate import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir codemate sunucusu başlatın.

```bash
codemate web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla codemate'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `CODEMATE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `codemate` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
codemate acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak       | Açıklama            |
| ------------ | ------------------- |
| `--cwd`      | Çalışma dizini      |
| `--port`     | Dinlenecek port     |
| `--hostname` | Dinlenecek host adı |

---

### uninstall

codemate'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
codemate uninstall
```

#### Bayraklar

| Bayrak          | Kısa | Tanım                                           |
| --------------- | ---- | ----------------------------------------------- |
| `--keep-config` | `-c` | Yapılandırma dosyalarını sakla                  |
| `--keep-data`   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| `--dry-run`     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| `--force`       | `-f` | Onay istemlerini atla                           |

---

### upgrade

codemate'u en son sürüme veya belirli bir sürüme günceller.

```bash
codemate upgrade [target]
```

En son sürüme yükseltmek için.

```bash
codemate upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
codemate upgrade v0.1.48
```

#### Bayraklar

| Bayrak     | Kısa | Açıklama                                               |
| ---------- | ---- | ------------------------------------------------------ |
| `--method` | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

codemate CLI aşağıdaki global bayrakları destekler.

| Bayrak         | Kısa | Tanım                                    |
| -------------- | ---- | ---------------------------------------- |
| `--help`       | `-h` | Yardımı görüntüle                        |
| `--version`    | `-v` | Sürüm numarasını yazdır                  |
| `--print-logs` |      | Günlükleri stderr'e yazdır               |
| `--log-level`  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

codemate ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `CODEMATE_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `CODEMATE_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `CODEMATE_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `CODEMATE_TUI_CONFIG`                 | string  | TUI yapılandırma dosyasının yolu                                            |
| `CODEMATE_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `CODEMATE_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `CODEMATE_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `CODEMATE_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `CODEMATE_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `CODEMATE_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `CODEMATE_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `CODEMATE_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `CODEMATE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `CODEMATE_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `CODEMATE_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `CODEMATE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `CODEMATE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `CODEMATE_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `CODEMATE_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `CODEMATE_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `CODEMATE_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `CODEMATE_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `CODEMATE_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `codemate`) |
| `CODEMATE_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `CODEMATE_EXPERIMENTAL`                         | boolean | Tüm deneysel özellikleri etkinleştir                    |
| `CODEMATE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `CODEMATE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `CODEMATE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `CODEMATE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `CODEMATE_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `CODEMATE_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `CODEMATE_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `CODEMATE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `CODEMATE_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `CODEMATE_EXPERIMENTAL_LSP_TY`                  | boolean | python dosyaları için TY LSP'yi etkinleştir             |
| `CODEMATE_EXPERIMENTAL_MARKDOWN`                | boolean | Deneysel işaretleme özelliklerini etkinleştir           |
| `CODEMATE_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
