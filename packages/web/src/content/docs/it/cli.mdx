---
title: CLI
description: Opzioni e comandi della CLI di codemate.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI di codemate, per impostazione predefinita, avvia la [TUI](/docs/tui) quando viene eseguita senza argomenti.

```bash
codemate
```

Ma accetta anche i comandi documentati in questa pagina. Questo ti permette di interagire con codemate in modo programmatico.

```bash
codemate run "Explain how closures work in JavaScript"
```

---

### tui

Avvia l'interfaccia testuale di codemate (interfaccia utente da terminale).

```bash
codemate [project]
```

#### Flag

| Flag                                     | Breve | Descrizione                                                              |
| ---------------------------------------- | ----- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                               |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Prompt da usare                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                       |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                          |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta su cui mettersi in ascolto                                         |
| <nobr><code>{"--hostname"}</code></nobr> |       | Hostname su cui mettersi in ascolto                                      |

---

## Comandi

La CLI di codemate include anche i seguenti comandi.

---

### agent

Gestisci gli agenti per codemate.

```bash
codemate agent [command]
```

---

### attach

Collega un terminale a un backend server di codemate già in esecuzione avviato tramite i comandi `serve` o `web`.

```bash
codemate attach [url]
```

Questo consente di usare la TUI con un backend codemate remoto. Per esempio:

```bash
# Start the backend server for web/mobile access
codemate web --port 4096 --hostname 0.0.0.0

# In un altro terminale, collega la TUI al backend in esecuzione
codemate attach http://10.20.30.40:4096
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory in cui avviare la TUI                                                         |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Crea un fork della sessione durante la continuazione (usa con `--continue` o `--session`)       |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `codemate_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `codemate_SERVER_USERNAME` o `codemate`) |

---

#### create

Crea un nuovo agente con configurazione personalizzata.

```bash
codemate agent create
```

Questo comando ti guida nella creazione di un nuovo agente con un system prompt personalizzato e configurazione degli strumenti.

---

#### list

Elenca tutti gli agenti disponibili.

```bash
codemate agent list
```

---

### auth

Comando per gestire le credenziali e il login dei provider.

```bash
codemate auth [command]
```

---

#### login

codemate si basa sull'elenco provider di [Models.dev](https://models.dev), quindi puoi usare `codemate auth login` per configurare le API key per qualunque provider tu voglia usare. Le credenziali vengono salvate in `~/.local/share/codemate/auth.json`.

```bash
codemate auth login
```

Quando codemate si avvia, carica i provider dal file delle credenziali e, se presenti, anche eventuali key definite nell'ambiente o in un file `.env` nel progetto.

---

#### list

Elenca tutti i provider autenticati come salvati nel file delle credenziali.

```bash
codemate auth list
```

Oppure la versione corta.

```bash
codemate auth ls
```

---

#### logout

Esegue il logout da un provider rimuovendolo dal file delle credenziali.

```bash
codemate auth logout
```

---

### github

Gestisci l'agente GitHub per l'automazione dei repository.

```bash
codemate github [command]
```

---

#### install

Installa l'agente GitHub nel tuo repository.

```bash
codemate github install
```

Questo configura il workflow GitHub Actions necessario e ti guida nel processo di configurazione. [Scopri di più](/docs/github).

---

#### run

Esegui l'agente GitHub. Tipicamente usato in GitHub Actions.

```bash
codemate github run
```

##### Flag

| Flag                                  | Descrizione                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Evento GitHub mock per cui eseguire l'agente |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                 |

---

### mcp

Gestisci i server Model Context Protocol.

```bash
codemate mcp [command]
```

---

#### add

Aggiungi un server MCP alla tua configurazione.

```bash
codemate mcp add
```

Questo comando ti guida nell'aggiunta di un server MCP locale o remoto.

---

#### list

Elenca tutti i server MCP configurati e il loro stato di connessione.

```bash
codemate mcp list
```

Oppure la versione corta.

```bash
codemate mcp ls
```

---

#### auth

Autentica con un server MCP con OAuth abilitato.

```bash
codemate mcp auth [name]
```

Se non fornisci un nome server, ti verrà chiesto di selezionare tra i server OAuth-capable disponibili.

Puoi anche elencare i server OAuth-capable e il loro stato di autenticazione.

```bash
codemate mcp auth list
```

Oppure la versione corta.

```bash
codemate mcp auth ls
```

---

#### logout

Rimuovi le credenziali OAuth per un server MCP.

```bash
codemate mcp logout [name]
```

---

#### debug

Esegui debug di problemi di connessione OAuth per un server MCP.

```bash
codemate mcp debug <name>
```

---

### models

Elenca tutti i modelli disponibili dai provider configurati.

```bash
codemate models [provider]
```

Questo comando mostra tutti i modelli disponibili tra i provider configurati nel formato `provider/model`.

È utile per capire l'esatto nome del modello da usare nella [config](/docs/config/).

Puoi anche passare opzionalmente un ID provider per filtrare i modelli a quel provider.

```bash
codemate models anthropic
```

#### Flag

| Flag                                    | Descrizione                                        |
| --------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Aggiorna la cache modelli da models.dev            |
| <nobr><code>{"--verbose"}</code></nobr> | Output più verboso (include metadati come i costi) |

Usa `--refresh` per aggiornare l'elenco modelli in cache. È utile quando nuovi modelli vengono aggiunti a un provider e vuoi vederli in codemate.

```bash
codemate models --refresh
```

---

### run

Esegui codemate in modalità non interattiva passando un prompt direttamente.

```bash
codemate run [message..]
```

È utile per scripting, automazione o quando vuoi una risposta rapida senza avviare la TUI completa. Per esempio:

```bash "codemate run"
codemate run Explain the use of context in Go
```

Puoi anche collegarti a una istanza `codemate serve` già in esecuzione per evitare i cold boot dei server MCP ad ogni esecuzione:

```bash
# Avvia un server headless in un terminale
codemate serve

# In un altro terminale, esegui comandi che si collegano ad esso
codemate run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | Il comando da eseguire; usa message per gli argomenti                                           |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`)                        |
| <nobr><code>{"--share"}</code></nobr>    |       | Condividi la sessione                                                                           |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                                              |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                                                 |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File da allegare al messaggio                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: default (formattato) o json (eventi JSON grezzi)                                       |
| <nobr><code>{"--title"}</code></nobr>    |       | Titolo sessione (usa prompt troncato se non viene fornito un valore)                            |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach a un server codemate in esecuzione (es. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `codemate_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `codemate_SERVER_USERNAME` o `codemate`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory di esecuzione, o percorso sul server remoto durante il collegamento                   |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante del modello (sforzo di ragionamento specifico del provider)                            |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostra blocchi di pensiero                                                                      |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta per il server locale (di default una porta casuale)                                       |

---

### serve

Avvia un server codemate headless per accesso via API. Vedi le [server docs](/docs/server) per l'interfaccia HTTP completa.

```bash
codemate serve
```

Avvia un server HTTP che espone accesso API alle funzionalità di codemate senza la TUI. Imposta `codemate_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `codemate`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### session

Gestisci le sessioni codemate.

```bash
codemate session [command]
```

---

#### list

Elenca tutte le sessioni codemate.

```bash
codemate session list
```

##### Flag

| Flag                                      | Breve | Descrizione                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limita alle N sessioni più recenti   |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato output: table o json (table) |

---

### stats

Mostra statistiche di utilizzo token e costo per le sessioni codemate.

```bash
codemate stats
```

#### Flag

| Flag                                    | Descrizione                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Mostra statistiche per gli ultimi N giorni (all time)                                 |
| <nobr><code>{"--tools"}</code></nobr>   | Numero di strumenti da mostrare (all)                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Mostra breakdown di utilizzo modelli (nascosto di default). Passa un numero per top N |
| <nobr><code>{"--project"}</code></nobr> | Filtra per progetto (tutti i progetti; stringa vuota: progetto corrente)              |

---

### export

Esporta i dati di sessione come JSON.

```bash
codemate export [sessionID]
```

Se non fornisci un ID sessione, ti verrà chiesto di selezionare tra le sessioni disponibili.

---

### import

Importa i dati di sessione da un file JSON o da un URL di condivisione codemate.

```bash
codemate import <file>
```

Puoi importare da un file locale o da un URL di condivisione codemate.

```bash
codemate import session.json
codemate import https://opncd.ai/s/abc123
```

---

### web

Avvia un server codemate headless con interfaccia web.

```bash
codemate web
```

Avvia un server HTTP e apre un browser per accedere a codemate tramite interfaccia web. Imposta `codemate_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `codemate`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### acp

Avvia un server ACP (Agent Client Protocol).

```bash
codemate acp
```

Questo comando avvia un server ACP che comunica via stdin/stdout usando nd-JSON.

#### Flag

| Flag                                     | Descrizione                         |
| ---------------------------------------- | ----------------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Directory di lavoro                 |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto    |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto |

---

### uninstall

Disinstalla codemate e rimuove tutti i file correlati.

```bash
codemate uninstall
```

#### Flag

| Flag                                        | Breve | Descrizione                                  |
| ------------------------------------------- | ----- | -------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Mantieni i file di configurazione            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Mantieni dati di sessione e snapshot         |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostra cosa verrebbe rimosso senza rimuovere |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Salta le richieste di conferma               |

---

### upgrade

Aggiorna codemate all'ultima versione o a una versione specifica.

```bash
codemate upgrade [target]
```

Per aggiornare all'ultima versione:

```bash
codemate upgrade
```

Per aggiornare a una versione specifica:

```bash
codemate upgrade v0.1.48
```

#### Flag

| Flag                                   | Breve | Descrizione                                               |
| -------------------------------------- | ----- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | Metodo di installazione usato: curl, npm, pnpm, bun, brew |

---

## Flag globali

La CLI di codemate accetta i seguenti flag globali.

| Flag                                       | Breve | Descrizione                            |
| ------------------------------------------ | ----- | -------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Mostra l'help                          |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Stampa il numero di versione           |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Stampa i log su stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Livello log (DEBUG, INFO, WARN, ERROR) |

---

## Variabili d'ambiente

codemate può essere configurato tramite variabili d'ambiente.

| Variabile                             | Tipo    | Descrizione                                                 |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `codemate_AUTO_SHARE`                 | boolean | Condivide automaticamente le sessioni                       |
| `codemate_GIT_BASH_PATH`              | string  | Percorso all'eseguibile Git Bash su Windows                 |
| `codemate_CONFIG`                     | string  | Percorso al file di configurazione                          |
| `codemate_TUI_CONFIG`                 | string  | Percorso al file di configurazione TUI                      |
| `codemate_CONFIG_DIR`                 | string  | Percorso alla directory di configurazione                   |
| `codemate_CONFIG_CONTENT`             | string  | Contenuto JSON di config inline                             |
| `codemate_DISABLE_AUTOUPDATE`         | boolean | Disabilita i controlli automatici di aggiornamento          |
| `codemate_DISABLE_PRUNE`              | boolean | Disabilita la potatura dei dati vecchi                      |
| `codemate_DISABLE_TERMINAL_TITLE`     | boolean | Disabilita aggiornamenti automatici del titolo terminale    |
| `codemate_PERMISSION`                 | string  | Config permessi JSON inline                                 |
| `codemate_DISABLE_DEFAULT_PLUGINS`    | boolean | Disabilita i plugin di default                              |
| `codemate_DISABLE_LSP_DOWNLOAD`       | boolean | Disabilita download automatico dei server LSP               |
| `codemate_ENABLE_EXPERIMENTAL_MODELS` | boolean | Abilita modelli sperimentali                                |
| `codemate_DISABLE_AUTOCOMPACT`        | boolean | Disabilita compaction automatica del contesto               |
| `codemate_DISABLE_CLAUDE_CODE`        | boolean | Disabilita lettura da `.claude` (prompt + skill)            |
| `codemate_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disabilita lettura di `~/.claude/CLAUDE.md`                 |
| `codemate_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disabilita caricamento di `.claude/skills`                  |
| `codemate_DISABLE_MODELS_FETCH`       | boolean | Disabilita fetch dei modelli da fonti remote                |
| `codemate_FAKE_VCS`                   | string  | Provider VCS finto per scopi di test                        |
| `codemate_CLIENT`                     | string  | Identificatore client (default `cli`)                       |
| `codemate_ENABLE_EXA`                 | boolean | Abilita gli strumenti di web search Exa                     |
| `codemate_SERVER_PASSWORD`            | string  | Abilita basic auth per `serve`/`web`                        |
| `codemate_SERVER_USERNAME`            | string  | Sovrascrive lo username basic auth (default `codemate`)     |
| `codemate_MODELS_URL`                 | string  | URL personalizzato per recuperare la configurazione modelli |

---

### Sperimentale

Queste variabili d'ambiente abilitano funzionalità sperimentali che potrebbero cambiare o essere rimosse.

| Variabile                                       | Tipo    | Descrizione                                |
| ----------------------------------------------- | ------- | ------------------------------------------ |
| `codemate_EXPERIMENTAL`                         | boolean | Abilita tutte le funzionalità sperimentali |
| `codemate_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Abilita icon discovery                     |
| `codemate_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disabilita copy on select nella TUI        |
| `codemate_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Timeout di default per comandi bash in ms  |
| `codemate_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Massimo token di output per risposte LLM   |
| `codemate_EXPERIMENTAL_FILEWATCHER`             | boolean | Abilita file watcher per l'intera dir      |
| `codemate_EXPERIMENTAL_OXFMT`                   | boolean | Abilita formatter oxfmt                    |
| `codemate_EXPERIMENTAL_LSP_TOOL`                | boolean | Abilita strumento LSP sperimentale         |
| `codemate_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disabilita file watcher                    |
| `codemate_EXPERIMENTAL_EXA`                     | boolean | Abilita funzionalità Exa sperimentali      |
| `codemate_EXPERIMENTAL_LSP_TY`                  | boolean | Abilita TY LSP per i file python           |
| `codemate_EXPERIMENTAL_MARKDOWN`                | boolean | Abilita markdown sperimentale              |
| `codemate_EXPERIMENTAL_PLAN_MODE`               | boolean | Abilita plan mode                          |
