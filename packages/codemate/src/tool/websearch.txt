- Search the web using Exa AI - performs real-time web searches and can scrape content from specific URLs
- Provides up-to-date information for current events and recent data
- Supports configurable result counts and returns the content from the most relevant websites
- Use this tool for accessing information beyond knowledge cutoff
- Searches are performed automatically within a single API call

You MUST use this tool when:
  - The user asks for latest, current, recent, today, now, pricing, release, version, availability, or news
  - The answer depends on vendor docs, public APIs, or facts that may have changed after training
  - You need to verify an external claim instead of answering from memory
  - You are not confident a web-dependent fact is still accurate
  - You hit unknown command/library errors and cannot confidently resolve from local context
  - Build/install/configuration failed and you are preparing a second substantial attempt
  - Two consecutive fixes for the same subproblem failed
  - selfcheck failed and you are preparing the next major fix attempt

Do NOT guess or rely on stale memory when this tool is available.

Troubleshooting trigger policy:
  - After two failed attempts on the same issue, websearch is mandatory before the third.
  - Use targeted queries including exact error text, tool name, version, platform, and current year.
  - Summarize what you learned from search before applying the next fix.

Usage notes:
  - Supports live crawling modes: 'fallback' (backup if cached unavailable) or 'preferred' (prioritize live crawling)
  - Search types: 'auto' (balanced), 'fast' (quick results), 'deep' (comprehensive search)
  - Configurable context length for optimal LLM integration
  - Domain filtering and advanced search options available

The current year is {{year}}. You MUST use this year when searching for recent information or current events
- Example: If the current year is 2026 and the user asks for "latest AI news", search for "AI news 2026", NOT "AI news 2025"
